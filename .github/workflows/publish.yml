name: Reusable workflow example

on:
  workflow_call:
    inputs:
      target:
        required: true
        type: string

jobs:
  triage:
    runs-on: self-hosted
    steps:
    - uses: actions/labeler@v4
      with:
        target: ${{ inputs.target }}
